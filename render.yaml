services:
  - type: web
    name: black-gold-pdf-bot

    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python bot.py

    plan: free

    autoDeploy: true
